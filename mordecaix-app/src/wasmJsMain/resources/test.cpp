#include <iostream>

// Define a function named 'add' that takes two integer parameters (a and b)
// and returns an integer.
int add(int a, int b) {
    // Return the sum of the two input integers.
    return a + b;
}